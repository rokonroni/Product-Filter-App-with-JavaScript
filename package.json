{
  "name": "microsoft-clone-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    
    "build" : "tailwindcss -i ./src/tailwind.css -o ./dist/style.css --watch",  
    "test": "echo \"Error: no test specified\" && exit 1"

   
  },
  "author": "rokon_roni",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  }
}
